<footer class="footer">
  <div class="container">
    <p class="text-muted"><%= t('.support') %> <br />
      <span class="glyphicon glyphicon-earphone" aria-hidden="true"></span>
      <a href="<%= t('.call_to') %>">
        <%= t('.phone') %>
      </a> <br />

      <span class="glyphicon glyphicon-envelope" aria-hidden="true"></span>
      <a href="<%= t('.email_to') %>">
        <%= t('.email') %></p>
      </a>
  </div>
</footer>
